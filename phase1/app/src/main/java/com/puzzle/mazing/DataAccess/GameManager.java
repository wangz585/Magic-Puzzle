package com.puzzle.mazing.DataAccess;
import java.util.ArrayList;
import java.util.List;

public class GameManager {
    /*
    private List<Game> myGames;

    GameManager() {
        myGames = new ArrayList<Game>();
    }

    void addGame(Game newGame) {
        myGames.add(newGame);
    }

    void removeGame(int id) {
        myGames.remove(getGame(id));
    }

    Game getGame(int id) {
        for (Game game: myGames) {
            if (game.getId() == id) {
                return game;
            }
        }
        return null;
    }

    List<Game> getAllGames() {
        return myGames;
    }

    boolean isFirstPlay(int id) {
        return getGame(id).getIsFirstPlay();
    }

     */
}

